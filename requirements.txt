# ---- ใช้ตอนรันจาก source (Python 3.14 ใน .venv — ดู README) ----
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24
customtkinter>=6.0.0
yt-dlp>=2026.8.19        # ✂ ตัดเสียงจาก YouTube (รันในโปรเซสลูก ไม่กินแรมโปรแกรมหลัก)
av>=17.0                 # PyAV — ffmpeg ถอดรหัสเฉพาะช่วงที่เลือก
pillow>=10.0             # วาดภาพความถี่ / คลื่นเสียง
livekit>=1.1             # 🎚 ตัดเสียงสะท้อน — WebRTC AEC3 (โหลดเฉพาะตอนเปิด)
soundcard>=0.4           # 🎚 ตัดเสียงสะท้อน — อ่านเสียงที่ลำโพงเล่นอยู่
segno>=1.6               # 📱 QR code สำหรับเชื่อมมือถือ
# ฮอตคีย์ใช้ Win32 RegisterHotKey ผ่าน ctypes — ไม่ต้องลงไลบรารีเพิ่ม

# ---- ใช้ตอนสร้าง .exe เท่านั้น (python build.py) ----
# pyinstaller>=6.0
# pillow>=10.0
