# ผลลัพธ์ตอน build
build/
dist/
*.spec
__pycache__/
*.pyc

# ค่าส่วนตัวของแต่ละเครื่อง (ชื่ออุปกรณ์เสียง, path ไฟล์ในเครื่อง)
config.json

# ไฟล์เสียงที่โหลดมาจากเว็บ — ไม่เอาขึ้น repo สาธารณะ
sounds/

# ซากจากการอัปเดตตัวเอง
*.old
*.new
*.part
